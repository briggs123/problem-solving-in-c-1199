#include<stdio.h>
#include<math.h>
int power(int a,int b)
{
    int  t,value;
    value= pow(a,t);
    return value;
}
int main()
{
    int i,j,conversion;
    printf("enter the base number =i \n enter the power =j");
    scanf("%d%d",&i,&j);
    conversion=power(i,j);
    printf("%d",conversion);
    return 0;
}
