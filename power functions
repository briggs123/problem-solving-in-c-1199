#include<stdio.h>
#include<math.h>
int power(int a,int b)
{
    int value;
    value= pow(a,b);
    return value;
}
int main()
{
    int i,j,conversion;
    printf("enter the base number =i \n enter the power =j");
    scanf("%d%d",&i,&j);
    conversion=power(i,j);
    printf("%d",conversion);
    return 0;
}
