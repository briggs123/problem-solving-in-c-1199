#include<stdio.h>
int main()
{
    int a;
    for(a=0;a<=100;a++)
    {
        if(a%2==0)
        {
            printf("even  %d",a);
        }
        else
        {
            printf("odd %d",a);
        }
    }
return 0;
}
